services:
  - type: web
    name: animal-classifier-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn animal_classifier.presentation.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
